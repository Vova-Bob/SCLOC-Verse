namespace SCLOCVerse.Helpers
{
    /// <summary>
    /// Ключі toast-аргументів (AddArgument / ParseToastArguments).
    /// Майбутнє розширення: Version, Environment, Action — без розбіжностей рядків.
    /// </summary>
    public static class ToastArgumentKeys
    {
        public const string Source = "source";
    }
}