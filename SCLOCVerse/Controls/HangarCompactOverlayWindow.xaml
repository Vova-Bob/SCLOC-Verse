<Window x:Class="SCLOCVerse.Controls.HangarCompactOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:hm="clr-namespace:SCLOCVerse.Models.HangarTimer"
        xmlns:cv="clr-namespace:SCLOCVerse.Helpers.Converters"
        Title="Hangar Compact Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        ShowActivated="False"
        SizeToContent="WidthAndHeight"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <Window.Resources>
        <cv:LightStateToBrushConverter x:Key="LightStateToBrush"/>
    </Window.Resources>

    <!-- Компактний бейдж: лише 5 LED + таймер.
         Без card-контейнера, без статусу, без підказок.
         Прозорий фон вікна — виден лише сам бейдж. -->
    <Border x:Name="Badge"
            Background="#CC0A1D29"
            BorderBrush="#2A5A78"
            BorderThickness="1.5"
            CornerRadius="16"
            Padding="16,8"
            Opacity="{Binding Opacity}">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

            <!-- 5 LED-індикаторів -->
            <ItemsControl ItemsSource="{Binding Lights}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type hm:HangarLightViewModel}">
                        <Ellipse Width="14"
                                 Height="14"
                                 Margin="0,0,10,0"
                                 Fill="{Binding State, Converter={StaticResource LightStateToBrush}}"
                                 Stroke="#557A7A7A"
                                 StrokeThickness="0.5"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Таймер -->
            <TextBlock Text="{Binding TimerText}"
                       FontFamily="Consolas"
                       FontSize="18"
                       FontWeight="Bold"
                       Foreground="#E0E8F0"
                       VerticalAlignment="Center"
                       Margin="14,0,0,0"/>
        </StackPanel>
    </Border>

</Window>