<UserControl x:Class="SCLOCVerse.Controls.SettingsHub.SettingsPlaceholderPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="IconGlyph" Grid.Row="0" Text="⚙" FontSize="56" Foreground="#2E5773" HorizontalAlignment="Center" Margin="0,0,0,16"/>

        <TextBlock x:Name="TitleText" Grid.Row="1" FontFamily="Segoe UI" FontSize="22" FontWeight="SemiBold" Foreground="#A7C6E7" HorizontalAlignment="Center"/>

        <TextBlock x:Name="MessageText" Grid.Row="2" FontFamily="Segoe UI" FontSize="14" Foreground="#6F8CA8" TextAlignment="Center" Margin="40,12,40,0" TextWrapping="Wrap" MaxWidth="520"/>
    </Grid>
</UserControl>
